NODE_ENV=development
PORT=4000

DATABASE_URL=postgresql://hris:hris@postgres:5432/hris?schema=public

REDIS_URL=redis://redis:6379

JWT_ACCESS_SECRET=change-me-access-secret
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_SECRET=change-me-refresh-secret
JWT_REFRESH_EXPIRES_IN=7d

MINIO_ENDPOINT=http://minio:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=hris-documents

# Kosongkan SMTP_HOST untuk mode dev (email hanya di-log, tidak benar-benar dikirim)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
MAIL_FROM=HRIS <no-reply@hris.local>

FRONTEND_URL=http://localhost:3000
